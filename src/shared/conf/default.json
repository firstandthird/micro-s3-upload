{
  "s3": {
    "bucket": "{{getEnv('S3_BUCKET')}}",
    "region": "{{getEnv('AWS_DEFAULT_REGION', 'us-east-1')}}"
  },
  "quality": 0.5,
  "siteTitle": "Micro Upload",
  "folderOriginals": "{{getEnv('FOLDER_ORIGINALS', 'uploads')}}",
  "folderOptimized": "{{getEnv('FOLDER_OPTIMIZED', 'optimized')}}",
  "cdn": "{{getEnv('CDN_HOST', '')}}",
  "allowedExtensions": "{{getEnv('ALLOWED_EXTENSIONS', '.jpg,.jpeg,.png,.svg')}}",
  "minimumImageSize": {
    "width": "{{getEnv('MIN_WIDTH', 50)}}",
    "height": "{{getEnv('MIN_HEIGHT', 50)}}"
  }
}
